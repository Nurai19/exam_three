1) Фреймворк - программная среда специального назначения, своеобразный каркас,
используемый для того, чтобы существенно облегчить процесс объединения определенных компонентов при создании программ
Фреймворк облегчает разработку и объеденение разных компонентов большого проекта

2) SQL - язык программирования структурированных запросов
Пример SQL кода:
CREATE DATABASE IF NOT EXISTS exam;
CREATE TABLE `table1` (`user_id` INT(5) NOT NULL AUTO_INCREMENT, `username` VARCHAR(50), PRIMARY KEY(`user_id`), INDEX(`username`));
CREATE TABLE `table2` (`phone_id` INT(5) NOT NULL AUTO_INCREMENT, `user_id` INT(5) NOT NULL, phone_number INT(10) NOT NULL, PRIMARY KEY (`phone_id`), INDEX(`user_id`, `phone_number`));
CREATE TABLE `table3` (`address_id` INT(5) NOT NULL AUTO_INCREMENT, `address_id` INT(5) NOT NULL, `float_number` INT(4) NOT NULL, PRIMARY KEY(`float_id`), INDEX(`float_id`, `room_number`));

3)ORM - технология программирования, которая связывает базы данных с концепциями объектно-ориентированных языков
программирования

4. ООП(объе́ктно-ориенти́рованное программи́рование, 'object-oriented programming') — метод программирования, основанный на представлении программы в виде совокупности объектов,
каждый из которых является экземпляром определённого класса, а классы образуют иерархию наследования.
Есть 4 принципа ООП:
1.Абстракция - придание объекту характеристик, которые отличают его от всех объектов
2.Инкапсуляция - сокрытие обьектов класса, делая их
недоступными
3.Наследование - наследование методов родительского класса
4.Полиморфизм - возможность объектов с одинаковой спецификацией
иметь различную реализацию

5) Агрегация - это отношение has-a "содержиться в", когда в классе есть какие-либо методы:
метод __init__ , get_number и т.д

Ассоциация - это отношение use-a "использует", когда класс ссылаетса на другой класс и использует его методы
